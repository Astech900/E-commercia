<% if(error && error.length) { %>
    <div class="alert alert-danger text-center alert-dismissible w-50 mt-2 "  role="alert">
   <%= error %> 
   <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
    <% } %>